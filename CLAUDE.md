# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業するときのガイドです。

## プロジェクト概要

「学習ログ」は、その日に学んだことを記録するシンプルな Web アプリです。
プログラミング学習の題材として作られており、作者は学習中の初心者です。

- 記録はブラウザの `localStorage` に保存される（サーバーは使わない）
- 入力欄にテキストを入れて「追加」ボタンを押すと、リストに追加・保存される
- ページを再読み込みしても、保存済みの記録が表示される

## ファイル構成

```
learning-log/
├── index.html   # アプリ本体（HTML / CSS / JavaScript がすべてこの1ファイルに入っている）
├── CLAUDE.md    # このファイル
└── docs/        # ドキュメント
```

## 実行方法

ビルドやインストールは不要。`index.html` をブラウザで開くだけで動く。

```bash
# 例: ローカルで簡易サーバーを立てる場合（任意）
python3 -m http.server 8000
# → http://localhost:8000 を開く
```

テストフレームワークやリンターは導入していない。動作確認はブラウザで手動で行う。

## コーディング方針

このリポジトリは学習用なので、**「動くこと」よりも「読んで理解できること」を優先**する。

- **言語**: UI・コメント・コミットメッセージはすべて日本語
- **技術**: 素の HTML / CSS / JavaScript のみ。フレームワークやビルドツールは導入しない
- **JavaScript のスタイル**:
  - 既存コードは `var` と `function` 宣言を使った初心者向けの書き方で統一されている
  - 新しいコードも既存のスタイルに合わせる（急に `const` / アロー関数 / クラスなどを混ぜない。導入するときは学習ステップとして説明を添える）
  - 1行ごとに何をしているか分かるよう、日本語コメントを丁寧に書く
- **変更の粒度**: 1コミット = 1つの学び、を目安に小さく変更する

## Claude への依頼で多いこと

- 機能追加（削除ボタン、日付表示、編集機能など）を「学習教材として」実装する
  - 実装するだけでなく、**何をしているかの説明コメント**を必ず添えること
- コードレビューと改善提案（初心者に分かる言葉で）
- 学習の振り返り・次に学ぶことの提案

## 自動化（Routine）について

このプロジェクトには、ユーザーがオフラインのときもクラウド上で自動実行される
定期タスク（Routine）が設定されている。詳細は `docs/AUTOMATION.md` を参照。
